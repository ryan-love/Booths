<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Homepage</title>
    <style>
        body{
            font-family: arial;
            margin: 0;
        }
        nav{
            background: #ff5353;
            padding: 20px 10px;
        }
        nav ul{
            max-width: 960px;
            margin: 0 auto;
            padding: 0;
        }
        nav li{
            list-style-type: none;
            display: inline-block;
            margin: 0 10px 0 0;
        }
        nav a{
            color: #fff;
            font-size: 18px;
            background: rgba(255,255,255,0.2);
            text-decoration: none;
            padding: 10px;
            display: block;
        }
        main, header{
            max-width: 960px;
            margin: 30px auto;
            padding: 0 10px;
            color: #333;
        }
        main img{
            width: 40px;
            margin-right: 10px;
            position: relative;
            top: 12px;
        }
        main p{
            display: inline-block;
        }
        .test{
            color: #ff5353;
        }
    </style>
</head>
<body>
<nav>
    <ul>
        <li><a href="/auth/logout">Log out</a></li>
        <li><a href="/auth/login">Login</a></li>
        <li><a href="/">Homepage</a></li>
    </ul>
</nav>
<header>
    <h1>Welcome to your profile, <%= (user.username) %></h1>
</header>
<main>
<img src="<%= (user.userImage) %>"/>


    <h1>Search Bookings</h1>
    <form action="/bookings" method="get">
        <label>Name</label>

        <input type="text" name="name" value="<%= search.name %>">
        <button type="submit">Submit</button>
    </form>
    <p>
        My Current Bookings
    <p class="test"> <%for(var i=0; i < booking.length; i++){ %> </p>
    <%=booking[i].name %>
    <% } %>

    </p>


</main>
</body>
</html>